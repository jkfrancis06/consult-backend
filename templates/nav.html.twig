{% extends 'base.html.twig' %}
{% block title %}Recueils{% endblock %}

{% block nav %}


    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand text-uppercase" href="{{ path('home') }}">recueils</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    {% if active == "home" %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('home') }}">Tableau de bord</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('home') }}">Tableau de bord</a>
                        </li>
                    {% endif %}



                    <li class="nav-item dropdown">
                        {% if active == "receuils" %}
                            <a class="nav-link dropdown-toggle active"  href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Recueils
                            </a>
                        {% else %}
                            <a class="nav-link dropdown-toggle"  href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Recueils
                            </a>
                        {% endif %}
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('receuils') }}" >Liste</a></li>
                            <li><a class="dropdown-item" href="{{ path('creer_receuils') }}">Ajouter des recueils</a></li>
                        </ul>
                    </li>

                    {% if active == "sources" %}
                        <a class="nav-link active" href="{{ path('sources') }}">Sources</a>

                    {% else %}
                        <a class="nav-link " href="{{ path('sources') }}">Sources</a>

                    {% endif %}
                    {% if active == "categories" %}
                        <a class="nav-link active" href="{{ path('categories') }}">Categories de recueil</a>

                    {% else %}
                        <a class="nav-link " href="{{ path('categories') }}">Categories de recueil</a>

                    {% endif %}

                    <!--<li class="nav-item dropdown">
                        {% if active == "user" %}
                            <a class="nav-link dropdown-toggle active"  href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Utilisateurs
                            </a>
                        {% else %}
                            <a class="nav-link dropdown-toggle"  href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Utilisateurs
                            </a>
                        {% endif %}
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('list_user') }}" >Liste</a></li>
                            <li><a class="dropdown-item" href="{{ path('add_user') }}">Ajouter un utilisateur</a></li>
                        </ul>
                    </li> -->

                    {% if active == "settings" %}
                        <li class="nav-item ">
                            <a class="nav-link active" href="{{ path('app_settings') }}">Parametres</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link " href="{{ path('app_settings') }}">Parametres</a>
                        </li>
                    {% endif %}


                    {% if is_granted('ROLE_ADMIN') == true %}

                        <li class="nav-item">
                            <a class="nav-link " href="{{ path('app_settings') }}">Espace Adminstration</a>
                        </li>

                    {% endif %}



                </ul>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Deconnexion</a>
            </div>
        </div>
    </nav>

{% endblock %}

{% block body %}{% endblock %}

