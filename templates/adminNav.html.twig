{% extends 'base.html.twig' %}
{% block title %}Administration{% endblock %}

{% block nav %}


    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand text-uppercase" href="{{ path('home') }}">recueils (Administration)</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    {% if active == "admin_home" %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('admin_home') }}">Tableau de bord</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('admin_home') }}">Tableau de bord</a>
                        </li>
                    {% endif %}

                    <li class="nav-item dropdown">
                        {% if active == "user" %}
                            <a class="nav-link dropdown-toggle active"  href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Utilisateurs
                            </a>
                        {% else %}
                            <a class="nav-link dropdown-toggle"  href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Utilisateurs
                            </a>
                        {% endif %}
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('list_user') }}" >Liste</a></li>
                            <li><a class="dropdown-item" href="{{ path('add_user') }}">Ajouter un utilisateur</a></li>
                        </ul>
                    </li>


                    {% if active == "admin_receuils" %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('admin_receuils') }}">Liste des recueils</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('admin_receuils') }}">Liste des recueils</a>
                        </li>
                    {% endif %}


                </ul>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Deconnexion</a>
            </div>
        </div>
    </nav>

{% endblock %}

{% block body %}{% endblock %}

